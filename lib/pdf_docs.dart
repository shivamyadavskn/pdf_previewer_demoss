import 'package:flutter/material.dart';

class PdfDoc extends StatelessWidget {
  const PdfDoc({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black12,
      child:Text("hello"),
    );
  }
}
